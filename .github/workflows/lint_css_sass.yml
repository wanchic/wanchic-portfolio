name: 🧹 CSS/Sass (stylelint)

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  stylelint:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version-file: ".nvmrc"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Run Stylelint
        run: npx stylelint -f verbose "**/*.{css,scss,sass}"

      - name: Run Prettier
        run: npx prettier "**/*.{css,scss}"
